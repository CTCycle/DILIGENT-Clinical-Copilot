{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local desktop window with no application IPC commands",
  "windows": ["main"],
  "permissions": ["core:default"]
}
