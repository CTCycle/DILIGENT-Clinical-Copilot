from __future__ import annotations

from DILIGENT.server.domain.settings.configuration import ServerSettings


class AppSettings(ServerSettings):
    """Backward-compatible alias for server JSON settings."""
