# PR #162 review validation

Date: 2026-09-08

Repository: `CTCycle/DILIGENT-Clinical-Copilot`

PR: [#162](https://github.com/CTCycle/DILIGENT-Clinical-Copilot/pull/162)

Base: `develop`

Reviewed head: `cc51af997d8d257b76ff6f114c566746262493b7`

## Integration

- The PR head was fetched and reviewed against the live `develop` ancestry.
- Local review fix: `9731f8f3da2b14d4d6a7cd4f9dfe73eb83c1fda5`.
- Local merge commit: `eef86bb16793fc1eca81dd0a798170dfad1d7420`.
- The PR head is an ancestor of local `develop`; the working tree is clean.
- No remote push or merge was performed. The GitHub PR remains open and draft.

## Review fixes

- Reconciled the page-specific RAG modal dimensions with the shared viewport-safe modal rules, including short-height margins, and removed obsolete overlay/header/close styles.
- Removed `embedding_device` from the persisted frontend `RagSettings` contract while retaining the runtime-only sanitizer and read-only runtime display.
- Updated affected fixtures and regression coverage to match the backend persistence contract.

## Validation

- Remote GitHub checks visible on the PR: `backend-quality`, `persistence-contract`, GitGuardian, and SonarCloud succeeded; `windows-regression` was skipped for pull-request events.
- Model-config page spec: 8 passed.
- Model-config core/state specs: 4 passed.
- Backend persistence unit test: 40 passed, 1 warning.
- `npm run build`: passed. Warning: the untouched clinical-sessions stylesheet exceeds its 20 kB component-style budget by 430 bytes.
- Full Angular suite: 65 passed, 1 failed across 15 files. The failure is the untouched patient-timetable source-evidence message assertion; it is unrelated to the PR files.
- Live contract check: a PUT containing persisted `embedding_device` was rejected with HTTP 422 as expected.
- Browser validation at 1280x720 and 1280x480 confirmed centered viewport-safe dialogs, internal body scrolling, visible headers/footers, and working Escape dismissal. Browser console errors and warnings were empty.
